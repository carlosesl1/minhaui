# Auditoria de baseline — 2026-07-15

## Escopo

Fotografia read-only do checkout canônico em
`C:\Users\Carlos\Documents\Codex\Minha UI APP`. Nenhum arquivo preexistente foi
apagado, movido, staged, commitado ou revertido durante o inventário.

## Identidade do snapshot

- Branch: `feature/windows-native-dock-v1`.
- HEAD: `2ce86c5853d6736d37b2f7ce0778e697d63ff664`.
- `codex/dock-task-manager` aponta para o mesmo commit.
- `main` aponta para `42f6f8f0e41390fbc0841c1df952dcfa2bd806f5`,
  commit de inicialização do repositório.

## Estado do checkout

Medições obtidas antes da criação destes documentos:

- `git status --porcelain=v1`: 181 entradas modificadas e 86 entradas `??`;
- `git diff --name-only`: 78 arquivos rastreados com diferença de conteúdo;
- `git diff --shortstat`: 7.411 inserções e 1.510 remoções nesses 78 arquivos;
- `git ls-files --others --exclude-standard`: 651 arquivos não rastreados.

Os números de `status` e `diff` não são equivalentes: o checkout também emite
avisos de normalização LF/CRLF, enquanto diretórios não rastreados aparecem como
uma entrada no status e contêm muitos arquivos. A estabilização deve classificar
os caminhos individualmente antes de qualquer ação.

## Gates observados

### Verde

- `cargo fmt --all --check`: exit code 0.

### Vermelho

- `cargo clippy --workspace --all-targets --all-features -- -D warnings`:
  6 ocorrências de `clippy::too_many_arguments` em `win32_owner.rs` e
  `win32_popover_render.rs`.
- `cargo test -p shell-platform-windows --all-features`: 49 aprovados e 3
  reprovados por depender de Calculator/Windows Terminal instalados.
- `cargo test --workspace --all-features`: interrompido por falha do teste de
  startup ao registrar a AppBar real do Windows.

Essas falhas não serão escondidas. Elas orientam a separação entre suíte
hermética e gate nativo e a redução da concentração no runtime.

## Riscos de governança

1. Não há baseline recente em `main` que represente o produto atual.
2. O volume do checkout impede revisão ou rollback confiável como uma unidade.
3. Evidências e artefatos temporários estão misturados ao inventário de fontes.
4. O contrato de CI documentado não está verde no snapshot atual.
5. Decisões arquiteturais anteriores não possuem ADRs.

## Saída exigida da estabilização

- classificar cada caminho como fonte durável, fixture, documento, evidência
  histórica ou artefato descartável;
- preservar todo trabalho até aprovação explícita da classificação;
- separar mudanças em incrementos coerentes e revisáveis;
- tornar o gate determinístico integralmente verde;
- mover validações dependentes da máquina para gate nativo documentado;
- validar build release a partir do commit candidato;
- estabelecer um commit de baseline limpo antes de retomar funcionalidades.

## Fora de escopo desta etapa

- limpar `.omo/` ou qualquer evidência;
- alterar `.gitignore`;
- reorganizar crates ou Modules;
- corrigir Clippy ou testes;
- stage, commit, merge ou atualização de `main`.

## Continuação da estabilização

O inventário detalhado posterior está em
[`2026-07-15-change-classification.md`](2026-07-15-change-classification.md).
Ele registra o crescimento do status causado pelos próprios documentos e scripts
de governança separadamente da fotografia original acima. Os resultados finais
dos gates serão adicionados ao documento de classificação sem reescrever esta
evidência histórica.
