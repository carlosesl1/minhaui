Task 2 failing-first workspace test
Captured: 2026-07-11T18:09:00-03:00

Command
-------
> $env:PATH="$env:USERPROFILE\.cargo\bin;$env:PATH"
> cargo test --workspace --all-features

Expected result
---------------
Exit code: 101

Each package compiled its empty library and its integration test failed with
error E0425 because the requested `crate_identity` function did not exist:

- shell-app
- shell-config
- shell-core
- shell-platform-windows
- shell-renderer
- shell-watchdog

Representative compiler output:

error[E0425]: cannot find function `crate_identity` in crate `shell_core`
 --> crates\shell-core\tests\identity.rs:5:32
  |
5 |     let identity = shell_core::crate_identity();
  |                                ^^^^^^^^^^^^^^ not found in `shell_core`

This is the intended RED failure: the test surface compiled far enough to prove
that implementation behavior, rather than a malformed workspace or import path,
was missing.
