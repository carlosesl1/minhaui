Task 4 corrected code review

Verdict: APPROVE after recovery and evidence corrections

Task 4a rebuild-presentation correction:
- Runtime instrumentation proved that generation-2 `create_surface` Presents returned `S_OK` and exposed nonblank pixels before `handle_event` performed its unconditional follow-up Present.
- That follow-up Present also returned `S_OK` but flipped each sequential swap chain to its untouched buffer, making both HWNDs uniform.
- The minimal correction removes the unconditional `present_all` fall-through. Rebuild actions already draw and Present inside `create_surface`; no second Present is valid without another draw.
- Skip/revert/restore runtime proof produced nonblank -> blank -> nonblank pixels at the exact topbar and dock HWNDs.
- No enum predicate or source-mirroring unit test was retained: it would remain green if a second Present were reintroduced. The accepted regression channel is the real failing-first pixel matrix plus the skip/revert/restore proof.

The earlier Task 4 review overstated confidence: a pure DeviceLifecycle enum test did not prove runtime recovery, native messages were consumed without rebuilding resources, and the first screenshot contained uncontrolled desktop pixels. Those claims are superseded by this receipt and the corrected artifacts.

Recovery review:
- WindowSurface::present classifies success, DXGI device removed/reset, and unexpected HRESULTs.
- D2DERR_RECREATE_TARGET is classified as recoverable at the shared HRESULT boundary.
- GetDeviceRemovedReason emits only error kind and hexadecimal HRESULT, never user or adapter data.
- RuntimeSurfaces owns renderer and both HWND-bound surfaces with Options, drops surfaces before renderer, recreates both, redraws, recommits, and continues the message loop.
- Window callbacks queue typed values through Mutex<VecDeque<PlatformEvent>>; no raw owner pointer, self-reference, or mutable global object is used.
- Device loss, DPI change, display change, power resume, and TaskbarCreated converge on the same RuntimeOrchestrator rebuild actions.
- Simulated device loss proves generation 1 to 2 on hardware and WARP. Lifecycle simulation proves generation 1 to 4.

Visual/design review:
- ShowcaseTokens is a focused transcription of DESIGN.md semantic colors and geometry.
- Dock radius is exactly 18.0; raw showcase colors and the earlier 22.0 radius were removed.
- The dock visibly covers rest, hover, pressed, active, focus-visible, unavailable, and error.
- Button, slider, device-row, calendar-cell, and popover primitives are represented without nested glass-card material.
- The corrected native screenshot uses a neutral privacy backdrop and contains no personal text.

Remove-ai-slops/overfit review:
- Tests target public behavior and shared orchestration state, not implementation source text.
- No deletion-only, tautological, duplicate, or exact-source-mirroring test was added.
- Recovery uses one event/action path instead of event-specific fake counters or QA-only renderer code.
- Simulation flags enter the same production rebuild functions as real events and are bounded to explicit QA launches.
- All Rust source files remain at or below 211 pure LOC; the largest physical file is 255 lines because required local SAFETY contracts are retained.

Residual risk:
- Live monitor verification was 96 DPI; DPI 120/144/192 and signed/offset work areas remain deterministic tests.
