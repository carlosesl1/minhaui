Task 3 verification receipt
Date: 2026-07-11 America/Sao_Paulo

PASS  cargo fmt --all -- --check
PASS  cargo clippy --workspace --all-targets --all-features -- -D warnings
PASS  cargo test --workspace (27 tests)
PASS  cargo test --workspace (27 tests, second run)
PASS  cargo build --workspace --release
PASS  cargo deny check (advisories, bans, licenses, sources; unmatched-license allowances warn only)
PASS  cargo run --release -p shell-config --example config_matrix (8/8 rows, exit 0)
PASS  scoped forbidden-pattern scan for unsafe blocks, unwrap, expect, panic, todo, unimplemented, allow

Adversarial coverage:
- Malformed and truncated JSON recover explicitly to validated safe defaults.
- Future and oversized documents are rejected before current-schema use.
- Stale same-directory temp/rollback paths are removed by the standard writer.
- Interrupted writer seam preserves original primary bytes.
- Corrupt primary falls back to the previous-valid backup.
- SHA-256 tampering and traversal/external asset references are rejected.
- Workspace tests passed twice without flakes.
- QA program validates artifacts as well as stdout and returns a failing exit code on any row.
- Temporary QA resources are removed and no child process is spawned.

N/A:
- Prompt injection: no model/browser/network content boundary exists in these crates.
- Hung command/process cleanup: all commands were bounded by a 120-second harness timeout and no background process is created.
- Strict single-operation Windows replacement: standard Rust lacks ReplaceFileW; the public writer seam and documented backup-first fallback carry this integration debt without unsafe/FFI in shell-config.
