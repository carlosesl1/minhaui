# Licensing notice

The Rust workspace is currently offered under `MIT OR Apache-2.0`, as declared in `Cargo.toml`. Dependency license validation is configured in `deny.toml`; release builds must pass that policy before distribution.

Windows, Steam, Apple, macOS, MyDockFinder, and third-party application names and marks belong to their respective owners. Reference screenshots under `docs/references/` are documentation-only and are excluded from every package.

No signing certificate, Steam credential, private key, or service token is stored in this repository or generated package layout.

